const nope = null;

export default {
  attached: nope,
  attribute: nope,
  created: nope,
  definedAttribute: 'defined',
  render: nope,
  detached: nope,
  events: nope,
  extends: nope,
  get observedAttributes () { return []; },
  props: nope,
  get prototype () { return {}; },
  ready: nope,
  renderedAttribute: 'rendered'
};
