import type from './type/element';

const nope = null;

export default {
  attached: nope,
  attribute: nope,
  created: nope,
  render: nope,
  detached: nope,
  events: nope,
  extends: nope,
  properties: nope,
  prototype: {},
  resolvedAttribute: 'resolved',
  ready: nope,
  type,
  unresolvedAttribute: 'unresolved'
};
