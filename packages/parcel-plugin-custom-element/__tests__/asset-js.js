const path = require('path');
const Asset = require('../src/asset-js');

// const asset = p =>
//   new Asset(path.join(__dirname, '..', '__asset__', p), {
//     minify: false,
//     rootDir: path.join(__dirname, '..', '__asset__')
//   });

test('parse', () => {
  // const a = asset('custom-element.js');
  // const p = await a.process();
});
